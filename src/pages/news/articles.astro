---
// src/pages/articles.astro
import Layout from '../layouts/Layout.astro';

// 安全地获取内容集合
let allArticles = [];

try {
  const { getCollection } = await import('astro:content');
  try {
    allArticles = await getCollection('articles');
    allArticles = allArticles.sort((a, b) => 
      new Date(b.data.publishDate || new Date()).getTime() - new Date(a.data.publishDate || new Date()).getTime()
    );
  } catch (error) {
    console.warn('Articles collection not found, using demo data');
    // 使用演示数据
    allArticles = [
      {
        slug: 'best-u-cards-2025',
        data: {
          title: '2025年最佳数字货币U卡推荐指南',
          description: '深度分析市场上最优秀的数字货币卡片，为您的选择提供专业指导',
          category: 'review',
          publishDate: new Date().toISOString(),
          author: '专业评测团队',
          readTime: '8分钟',
          tags: ['U卡', '评测', '推荐', '2025'],
          featured: true
        }
      },
      {
        slug: 'crypto-card-security-guide',
        data: {
          title: '数字货币卡片安全使用完全指南',
          description: '从申请到使用，全面了解如何安全地使用数字货币卡片',
          category: 'guide',
          publishDate: new Date(Date.now() - 86400000).toISOString(),
          author: '安全专家',
          readTime: '12分钟',
          tags: ['安全', '指南', '风险防范'],
          featured: false
        }
      },
      {
        slug: 'market-analysis-q1-2025',
        data: {
          title: '2025年Q1数字货币市场分析报告',
          description: '详细分析第一季度数字货币市场趋势和投资机会',
          category: 'news',
          publishDate: new Date(Date.now() - 172800000).toISOString(),
          author: '市场分析师',
          readTime: '15分钟',
          tags: ['市场分析', '趋势', '投资'],
          featured: true
        }
      },
      {
        slug: 'defi-integration-cards',
        data: {
          title: 'DeFi与传统卡片的完美结合',
          description: '探索去中心化金融如何改变传统支付卡片行业',
          category: 'analysis',
          publishDate: new Date(Date.now() - 259200000).toISOString(),
          author: 'DeFi研究员',
          readTime: '10分钟',
          tags: ['DeFi', '创新', '金融科技'],
          featured: false
        }
      }
    ];
  }
} catch (error) {
  console.error('Error loading articles:', error);
}

// 按类别分组文章
const articlesByCategory = allArticles.reduce((acc, article) => {
  const category = article.data.category || 'other';
  if (!acc[category]) acc[category] = [];
  acc[category].push(article);
  return acc;
}, {});

const featuredArticles = allArticles.filter(article => article.data.featured);
---

<Layout title="资讯文章 - 最新的数字货币行业动态与深度分析">
  <div class="min-h-screen bg-gradient-to-br from-slate-50 via-purple-50 to-indigo-100">
    <!-- Hero Section -->
    <section class="relative overflow-hidden bg-gradient-to-br from-purple-600 via-indigo-600 to-blue-700 py-20">
      <div class="absolute inset-0">
        <div class="absolute top-0 left-0 w-96 h-96 bg-white/10 rounded-full blur-3xl -translate-x-48 -translate-y-48"></div>
        <div class="absolute bottom-0 right-0 w-96 h-96 bg-purple-300/20 rounded-full blur-3xl translate-x-48 translate-y-48"></div>
      </div>
      
      <div class="relative max-w-7xl mx-auto px-6 lg:px-8 text-center">
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-white/20 backdrop-blur-sm border border-white/30 text-white/90 text-sm font-medium mb-8">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
          </svg>
          专业资讯
        </div>
        
        <h1 class="text-5xl lg:text-6xl font-bold text-white mb-6">
          数字货币
          <span class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-orange-400">
            行业资讯
          </span>
        </h1>
        
        <p class="text-xl text-white/80 mb-8 max-w-3xl mx-auto">
          深度解读行业动态，专业分析市场趋势，为您的投资决策提供有价值的参考
        </p>
        
        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/20">
            <div class="text-2xl font-bold text-white">{allArticles.length}+</div>
            <div class="text-white/70 text-sm">原创文章</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/20">
            <div class="text-2xl font-bold text-white">Daily</div>
            <div class="text-white/70 text-sm">每日更新</div>
          </div>
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 border border-white/20">
            <div class="text-2xl font-bold text-white">Expert</div>
            <div class="text-white/70 text-sm">专家团队</div>
          </div>
        </div>
      </div>
    </section>

    <!-- 筛选和搜索 -->
    <section class="py-12 bg-white border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="flex flex-col lg:flex-row gap-6 items-center">
          <!-- 搜索框 -->
          <div class="flex-1 max-w-md">
            <div class="relative">
              <svg class="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
              </svg>
              <input 
                type="text" 
                placeholder="搜索文章标题或关键词..."
                class="w-full pl-10 pr-4 py-3 border border-gray-200 rounded-xl focus:border-purple-500 focus:ring-2 focus:ring-purple-200 outline-none transition-all"
                id="search-input"
              />
            </div>
          </div>
          
          <!-- 筛选器 -->
          <div class="flex flex-wrap gap-4">
            <!-- 分类筛选 -->
            <select class="px-4 py-3 border border-gray-200 rounded-xl focus:border-purple-500 focus:ring-2 focus:ring-purple-200 outline-none" id="category-filter">
              <option value="">所有分类</option>
              <option value="review">评测推荐</option>
              <option value="guide">使用指南</option>
              <option value="news">行业新闻</option>
              <option value="analysis">深度分析</option>
            </select>
            
            <!-- 排序 -->
            <select class="px-4 py-3 border border-gray-200 rounded-xl focus:border-purple-500 focus:ring-2 focus:ring-purple-200 outline-none" id="sort-filter">
              <option value="date">按发布时间</option>
              <option value="title">按标题排序</option>
              <option value="readtime">按阅读时长</option>
            </select>
          </div>
        </div>
      </div>
    </section>

    <!-- 精选文章 -->
    {featuredArticles.length > 0 && (
      <section class="py-16 bg-gradient-to-r from-purple-50 to-indigo-50">
        <div class="max-w-7xl mx-auto px-6 lg:px-8">
          <div class="text-center mb-12">
            <div class="inline-flex items-center px-4 py-2 rounded-full bg-purple-100 text-purple-700 text-sm font-medium mb-4">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
              </svg>
              精选推荐
            </div>
            <h2 class="text-4xl font-bold text-gray-900 mb-4">编辑精选</h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
              由我们的专业编辑团队精心挑选的优质内容
            </p>
          </div>
          
          <div class="grid gap-8 md:grid-cols-2">
            {featuredArticles.slice(0, 2).map((article) => (
              <article class="group bg-white rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 overflow-hidden">
                <!-- 文章头图 -->
                <div class="h-64 bg-gradient-to-br from-purple-400 to-indigo-600 relative overflow-hidden">
                  <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
                  
                  <!-- 分类标签 -->
                  <div class="absolute top-4 left-4">
                    <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                      article.data.category === 'news' ? 'bg-red-100 text-red-800' :
                      article.data.category === 'guide' ? 'bg-blue-100 text-blue-800' :
                      article.data.category === 'review' ? 'bg-green-100 text-green-800' :
                      'bg-purple-100 text-purple-800'
                    }`}>
                      {article.data.category === 'review' ? '评测推荐' :
                       article.data.category === 'guide' ? '使用指南' :
                       article.data.category === 'news' ? '行业新闻' :
                       article.data.category === 'analysis' ? '深度分析' :
                       '资讯'}
                    </span>
                  </div>
                  
                  <!-- 精选标识 -->
                  <div class="absolute top-4 right-4">
                    <div class="bg-yellow-400 text-yellow-900 px-2 py-1 rounded-full text-xs font-bold">
                      精选
                    </div>
                  </div>
                  
                  <!-- 发布时间 -->
                  <div class="absolute bottom-4 left-4 text-white/90 text-sm">
                    {new Date(article.data.publishDate).toLocaleDateString('zh-CN')}
                  </div>
                  
                  <!-- 阅读时间 -->
                  <div class="absolute bottom-4 right-4 text-white/90 text-sm">
                    {article.data.readTime || '5分钟'}
                  </div>
                </div>
                
                <div class="p-8">
                  <h3 class="text-2xl font-bold mb-4 line-clamp-2 group-hover:text-purple-600 transition-colors">
                    <a href={`/articles/${article.slug}`}>
                      {article.data.title}
                    </a>
                  </h3>
                  
                  <p class="text-gray-600 mb-4 line-clamp-3 leading-relaxed">
                    {article.data.description}
                  </p>
                  
                  <!-- 标签 -->
                  {article.data.tags && (
                    <div class="flex flex-wrap gap-2 mb-4">
                      {article.data.tags.slice(0, 3).map((tag) => (
                        <span class="px-2 py-1 bg-purple-50 text-purple-700 rounded-md text-xs">
                          #{tag}
                        </span>
                      ))}
                    </div>
                  )}
                  
                  <!-- 作者和阅读按钮 -->
                  <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                    <div class="flex items-center space-x-3">
                      <div class="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center">
                        <svg class="w-4 h-4 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                        </svg>
                      </div>
                      <span class="text-sm text-gray-600">{article.data.author}</span>
                    </div>
                    
                    <a 
                      href={`/articles/${article.slug}`}
                      class="inline-flex items-center text-purple-600 hover:text-purple-800 font-medium text-sm group-hover:translate-x-1 transition-transform"
                    >
                      阅读全文 
                      <svg class="ml-1 w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                      </svg>
                    </a>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- 所有文章 -->
    <section class="py-16">
      <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">全部文章</h2>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            浏览我们的完整文章库，找到您感兴趣的内容
          </p>
        </div>
        
        <div id="articles-container">
          {allArticles.length > 0 ? (
            <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3" id="articles-grid">
              {allArticles.map((article) => (
                <article class="group bg-white rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-300 overflow-hidden article-item" 
                         data-title={article.data.title.toLowerCase()}
                         data-category={article.data.category}
                         data-date={article.data.publishDate}
                         data-readtime={article.data.readTime}>
                  
                  <!-- 文章头图 -->
                  <div class="h-48 bg-gradient-to-br from-indigo-400 to-purple-500 relative overflow-hidden">
                    <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
                    
                    <!-- 分类标签 -->
                    <div class="absolute top-4 left-4">
                      <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                        article.data.category === 'news' ? 'bg-red-100 text-red-800' :
                        article.data.category === 'guide' ? 'bg-blue-100 text-blue-800' :
                        article.data.category === 'review' ? 'bg-green-100 text-green-800' :
                        'bg-purple-100 text-purple-800'
                      }`}>
                        {article.data.category === 'review' ? '评测推荐' :
                         article.data.category === 'guide' ? '使用指南' :
                         article.data.category === 'news' ? '行业新闻' :
                         article.data.category === 'analysis' ? '深度分析' :
                         '资讯'}
                      </span>
                    </div>
                    
                    <!-- 发布时间 -->
                    <div class="absolute bottom-4 right-4 text-white/80 text-sm">
                      {new Date(article.data.publishDate).toLocaleDateString('zh-CN')}
                    </div>
                  </div>
                  
                  <div class="p-6">
                    <h3 class="text-xl font-bold mb-3 line-clamp-2 group-hover:text-purple-600 transition-colors">
                      <a href={`/articles/${article.slug}`}>
                        {article.data.title}
                      </a>
                    </h3>
                    
                    <p class="text-gray-600 mb-4 line-clamp-3 text-sm leading-relaxed">
                      {article.data.description}
                    </p>
                    
                    <!-- 标签 -->
                    {article.data.tags && (
                      <div class="flex flex-wrap gap-1 mb-4">
                        {article.data.tags.slice(0, 2).map((tag) => (
                          <span class="px-2 py-1 bg-purple-50 text-purple-700 rounded-md text-xs">
                            #{tag}
                          </span>
                        ))}
                      </div>
                    )}
                    
                    <!-- 底部信息 -->
                    <div class="flex items-center justify-between pt-4 border-t border-gray-100">
                      <div class="flex items-center space-x-2 text-xs text-gray-500">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        <span>{article.data.readTime || '5分钟'}</span>
                      </div>
                      
                      <a 
                        href={`/articles/${article.slug}`}
                        class="inline-flex items-center text-purple-600 hover:text-purple-800 font-medium text-sm group-hover:translate-x-1 transition-transform"
                      >
                        阅读更多 
                        <svg class="ml-1 w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                        </svg>
                      </a>
                    </div>
                  </div>
                </article>
              ))}
            </div>
          ) : (
            <div class="text-center py-16">
              <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
                <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"/>
                </svg>
              </div>
              <h3 class="text-2xl font-bold text-gray-900 mb-2">暂无文章内容</h3>
              <p class="text-gray-600">我们正在准备精彩的内容，敬请期待</p>
            </div>
          )}
        </div>
        
        <!-- 无搜索结果提示 -->
        <div id="no-results" class="text-center py-16 hidden">
          <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-2">未找到匹配的文章</h3>
          <p class="text-gray-600">请尝试调整搜索条件或筛选选项</p>
        </div>
      </div>
    </section>

    <!-- 订阅专区 -->
    <section class="py-16 bg-gradient-to-r from-purple-600 to-indigo-600">
      <div class="max-w-4xl mx-auto px-6 lg:px-8 text-center">
        <h2 class="text-4xl font-bold text-white mb-6">
          订阅我们的资讯简报
        </h2>
        <p class="text-xl text-white/80 mb-8">
          每周为您精选最有价值的行业资讯和深度分析
        </p>
        
        <div class="bg-white/10 backdrop-blur-sm rounded-3xl p-8 mb-8 border border-white/20">
          <form class="flex flex-col sm:flex-row gap-4 max-w-lg mx-auto">
            <input 
              type="email" 
              placeholder="请输入您的邮箱地址" 
              class="flex-1 px-6 py-4 rounded-2xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-white/40 focus:ring-2 focus:ring-white/20 outline-none transition-all"
            />
            <button 
              type="submit"
              class="px-8 py-4 bg-white text-purple-600 rounded-2xl hover:bg-gray-100 transform hover:-translate-y-1 transition-all duration-300 font-semibold shadow-lg hover:shadow-xl whitespace-nowrap"
            >
              立即订阅
            </button>
          </form>
          <p class="text-sm text-white/60 mt-4">
            订阅即表示您同意我们的隐私政策，我们承诺保护您的个人信息
          </p>
        </div>
        
        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <a 
            href="/contact" 
            class="inline-flex items-center px-8 py-4 bg-white/10 backdrop-blur-sm border border-white/20 text-white rounded-2xl hover:bg-white/20 transition-all font-semibold"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            联系编辑团队
          </a>
        </div>
      </div>
    </section>
  </div>
</Layout>

<script>
  // 搜索和筛选功能
  document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('search-input');
    const categoryFilter = document.getElementById('category-filter');
    const sortFilter = document.getElementById('sort-filter');
    const articlesGrid = document.getElementById('articles-grid');
    const noResults = document.getElementById('no-results');
    
    let allArticles = Array.from(document.querySelectorAll('.article-item'));
    
    function filterAndSort() {
      const searchTerm = searchInput.value.toLowerCase();
      const category = categoryFilter.value;
      const sortBy = sortFilter.value;
      
      // 筛选文章
      let filteredArticles = allArticles.filter(article => {
        const title = article.dataset.title;
        const articleCategory = article.dataset.category;
        
        const matchesSearch = !searchTerm || title.includes(searchTerm);
        const matchesCategory = !category || articleCategory === category;
        
        return matchesSearch && matchesCategory;
      });
      
      // 排序
      filteredArticles.sort((a, b) => {
        switch (sortBy) {
          case 'title':
            return a.dataset.title.localeCompare(b.dataset.title);
          case 'date':
            return new Date(b.dataset.date).getTime() - new Date(a.dataset.date).getTime();
          case 'readtime':
            const aTime = parseInt(a.dataset.readtime) || 5;
            const bTime = parseInt(b.dataset.readtime) || 5;
            return aTime - bTime;
          default:
            return new Date(b.dataset.date).getTime() - new Date(a.dataset.date).getTime();
        }
      });
      
      // 显示结果
      if (filteredArticles.length === 0) {
        articlesGrid.style.display = 'none';
        noResults.classList.remove('hidden');
      } else {
        articlesGrid.style.display = 'grid';
        noResults.classList.add('hidden');
        
        // 重新排列文章
        allArticles.forEach(article => article.style.display = 'none');
        filteredArticles.forEach(article => article.style.display = 'block');
      }
    }
    
    // 绑定事件监听器
    searchInput.addEventListener('input', filterAndSort);
    categoryFilter.addEventListener('change', filterAndSort);
    sortFilter.addEventListener('change', filterAndSort);