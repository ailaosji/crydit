---
// src/pages/cards/[slug].astro
import { type CollectionEntry, getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import { Image } from 'astro:assets';
import GiscusComments from '../../components/GiscusComments.astro';

export async function getStaticPaths() {
  const cards = await getCollection('cards');
  return cards.map((card) => ({
    params: { slug: card.slug },
    props: card,
  }));
}

type Props = CollectionEntry<'cards'>;

const card = Astro.props;
const { title, name, description, cardType, isVirtual, isPhysical, pros, cons, features, limits, rewards, issuer, supportedRegions, supportedCurrencies, virtualCardPrice, physicalCardPrice, depositFee, transactionFee, withdrawalFee, atmFee, annualFee, affiliateLink } = card.data;

// 获取相关U卡
let relatedCards = [];
if (card.data.relatedCards) {
  const allCards = await getCollection('cards');
  relatedCards = allCards.filter(c => 
    card.data.relatedCards?.includes(c.slug) && c.slug !== card.slug
  ).slice(0, 3);
}

// 渲染Markdown内容
const { Content } = await card.render();

// 生成结构化数据（移除评分相关）
const structuredData = {
  "@context": "https://schema.org",
  "@type": "Product",
  "name": name,
  "description": description,
  "brand": {
    "@type": "Brand",
    "name": issuer
  },
  "offers": {
    "@type": "Offer",
    "price": virtualCardPrice || physicalCardPrice || "0",
    "priceCurrency": "USD",
    "availability": "https://schema.org/InStock"
  },
  "category": "Financial Services",
  "additionalProperty": [
    {
      "@type": "PropertyValue",
      "name": "Card Type",
      "value": cardType
    },
    {
      "@type": "PropertyValue", 
      "name": "Virtual Card",
      "value": isVirtual ? "Yes" : "No"
    },
    {
      "@type": "PropertyValue",
      "name": "Physical Card", 
      "value": isPhysical ? "Yes" : "No"
    }
  ]
};
---

<Layout title={title || `${name} - 全球虚拟信用卡评测`}>
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  
  <div class="min-h-screen bg-gradient-to-br from-gray-50 to-indigo-50 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <!-- Hero Section with Card Image -->
      <div class="bg-white rounded-3xl shadow-2xl overflow-hidden mb-12">
        <div class="relative">
          <!-- Background Gradient -->
          <div class="absolute inset-0 bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-500"></div>
          
          <!-- Content Container -->
          <div class="relative p-8 lg:p-12">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
              
              <!-- Left: Card Info -->
              <div class="text-white">
                <div class="flex flex-wrap items-center gap-3 mb-6">
                  <span class={`px-4 py-2 rounded-full text-sm font-semibold ${
                    cardType === 'visa' ? 'bg-blue-500' : 'bg-red-500'
                  } text-white shadow-lg`}>
                    {cardType?.toUpperCase()}
                  </span>
                  {isVirtual && (
                    <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-medium">
                      🔮 虚拟卡
                    </span>
                  )}
                  {isPhysical && (
                    <span class="px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-medium">
                      💳 实体卡
                    </span>
                  )}
                </div>
                
                <h1 class="text-4xl lg:text-5xl font-bold mb-4 leading-tight">{name}</h1>
                <p class="text-lg lg:text-xl text-white/90 mb-6 leading-relaxed">{description}</p>
                
                <!-- Quick Stats -->
                <div class="grid grid-cols-2 gap-4 pt-6 border-t border-white/20">
                  <div>
                    <p class="text-white/70 text-sm mb-1">发卡机构</p>
                    <p class="text-white font-semibold">{issuer}</p>
                  </div>
                  <div>
                    <p class="text-white/70 text-sm mb-1">支持地区</p>
                    <p class="text-white font-semibold">
                      {supportedRegions?.slice(0, 3).join(', ')}{supportedRegions?.length > 3 ? '...' : ''}
                    </p>
                  </div>
                </div>
              </div>
              
              <!-- Right: Card Visual -->
              <div class="relative">
                <!-- Card Container -->
                <div class="relative mx-auto max-w-sm transform hover:scale-105 transition-transform duration-300">
                  <!-- Card Shadow -->
                  <div class="absolute inset-0 bg-black/20 rounded-2xl blur-xl transform translate-y-4"></div>
                  
                  <!-- Card Design -->
                  <div class="relative bg-gradient-to-br from-gray-900 to-gray-700 rounded-2xl p-6 aspect-[1.586/1] flex flex-col justify-between shadow-2xl">
                    <!-- Card Chip -->
                    <div class="flex justify-between items-start">
                      <div class="w-12 h-10 bg-gradient-to-br from-yellow-400 to-yellow-600 rounded-md"></div>
                      <div class="text-right">
                        {cardType === 'visa' ? (
                          <span class="text-white text-2xl font-bold italic">VISA</span>
                        ) : (
                          <span class="text-white text-2xl font-bold">Mastercard</span>
                        )}
                      </div>
                    </div>
                    
                    <!-- Card Number -->
                    <div class="space-y-2">
                      <div class="flex space-x-3 text-white/80 text-lg font-mono">
                        <span>••••</span>
                        <span>••••</span>
                        <span>••••</span>
                        <span>••••</span>
                      </div>
                    </div>
                    
                    <!-- Card Bottom -->
                    <div class="flex justify-between items-end">
                      <div>
                        <p class="text-white/60 text-xs uppercase mb-1">Card Holder</p>
                        <p class="text-white font-medium">{name}</p>
                      </div>
                      <div class="text-right">
                        <p class="text-white/60 text-xs uppercase mb-1">Valid Thru</p>
                        <p class="text-white font-medium">••/••</p>
                      </div>
                    </div>
                    
                    <!-- Card Pattern -->
                    <div class="absolute inset-0 rounded-2xl opacity-10">
                      <div class="absolute top-0 right-0 w-40 h-40 bg-white rounded-full -translate-y-20 translate-x-20"></div>
                      <div class="absolute bottom-0 left-0 w-32 h-32 bg-white rounded-full translate-y-16 -translate-x-16"></div>
                    </div>
                  </div>
                  
                  <!-- Floating Elements -->
                  <div class="absolute -top-4 -right-4 bg-green-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                    推荐
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Main Content Grid -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        <!-- Main Content -->
        <div class="lg:col-span-2 space-y-8">
          
          <!-- 重新设计的手续费部分 -->
          <section class="bg-white rounded-3xl shadow-xl p-8 mb-8">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">手续费信息</h2>
            
            <!-- 卡片类型对比 -->
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
              
              <!-- 实体卡 -->
              {isPhysical && (
                <div class="bg-gradient-to-br from-blue-50 to-indigo-100 rounded-3xl p-8 relative overflow-hidden">
                  <!-- 装饰性元素 -->
                  <div class="absolute top-0 right-0 w-32 h-32 bg-blue-200 rounded-full -translate-y-16 translate-x-16 opacity-20"></div>
                  
                  <!-- 卡片头部 -->
                  <div class="flex items-center mb-8">
                    <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-2xl flex items-center justify-center mr-4 shadow-lg">
                      <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"/>
                      </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800">实体卡</h3>
                  </div>
                  
                  <!-- 费用信息网格 -->
                  <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">开卡费用</p>
                      <p class="text-2xl font-bold text-blue-600">
                        {physicalCardPrice ? `${physicalCardPrice}` : "免费"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">充值手续费</p>
                      <p class="text-2xl font-bold text-gray-800">
                        {depositFee || "免费"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">交易手续费</p>
                      <p class="text-2xl font-bold text-gray-800">
                        {transactionFee || "0.9%"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">提现手续费</p>
                      <p class="text-xl font-bold text-gray-800">
                        {withdrawalFee || (
                          <div>
                            <div class="text-sm text-green-600 font-medium mb-1">每月前5次免费，之后</div>
                            <div>€0.90/次</div>
                          </div>
                        )}
                      </p>
                    </div>
                  </div>

                  <!-- 其他信息 -->
                  <div class="space-y-4 text-gray-700">
                    <div class="flex justify-between items-center">
                      <span class="font-medium">配送时间</span>
                      <span class="text-gray-600">7-15个工作日</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium">ATM取现费</span>
                      <span class="text-gray-600">{atmFee || "€0.90/次"}</span>
                    </div>
                  </div>
                </div>
              )}

              <!-- 虚拟卡 -->
              {isVirtual && (
                <div class="bg-gradient-to-br from-purple-50 to-pink-100 rounded-3xl p-8 relative overflow-hidden">
                  <!-- 装饰性元素 -->
                  <div class="absolute top-0 right-0 w-32 h-32 bg-purple-200 rounded-full -translate-y-16 translate-x-16 opacity-20"></div>
                  
                  <!-- 卡片头部 -->
                  <div class="flex items-center mb-8">
                    <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-pink-600 rounded-2xl flex items-center justify-center mr-4 shadow-lg">
                      <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"/>
                      </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800">虚拟卡</h3>
                  </div>
                  
                  <!-- 费用信息网格 -->
                  <div class="grid grid-cols-2 gap-4 mb-8">
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">开卡费用</p>
                      <p class="text-2xl font-bold text-purple-600">
                        {virtualCardPrice ? `${virtualCardPrice}` : "免费"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">充值手续费</p>
                      <p class="text-2xl font-bold text-gray-800">
                        {depositFee || "免费"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">交易手续费</p>
                      <p class="text-2xl font-bold text-gray-800">
                        {transactionFee || "0.9%"}
                      </p>
                    </div>
                    <div class="bg-white rounded-2xl p-6 text-center shadow-sm hover:shadow-md transition-shadow">
                      <p class="text-gray-600 text-sm mb-2 font-medium">提现手续费</p>
                      <p class="text-xl font-bold text-gray-800">
                        {withdrawalFee || (
                          <div>
                            <div class="text-sm text-green-600 font-medium mb-1">每月前5次免费，之后</div>
                            <div>€0.90/次</div>
                          </div>
                        )}
                      </p>
                    </div>
                  </div>

                  <!-- 其他信息 -->
                  <div class="space-y-4 text-gray-700">
                    <div class="flex justify-between items-center">
                      <span class="font-medium">即时激活</span>
                      <span class="text-green-600 font-medium flex items-center">
                        <svg class="w-5 h-5 mr-1" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                        支持
                      </span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium">使用场景</span>
                      <span class="text-gray-600">在线支付</span>
                    </div>
                  </div>
                </div>
              )}
            </div>
            
            <!-- 支持的货币展示 -->
            {supportedCurrencies && supportedCurrencies.length > 0 && (
              <div class="mt-8 p-8 bg-gradient-to-r from-gray-50 to-blue-50 rounded-3xl">
                <div class="flex items-center mb-6">
                  <div class="w-12 h-12 bg-blue-500 rounded-xl flex items-center justify-center mr-4">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
                    </svg>
                  </div>
                  <h4 class="text-xl font-semibold text-gray-800">支持货币</h4>
                </div>
                <div class="grid grid-cols-3 md:grid-cols-6 lg:grid-cols-8 gap-4">
                  {supportedCurrencies.map((currency) => (
                    <div class="bg-white rounded-xl px-4 py-3 text-center shadow-sm hover:shadow-md transition-shadow">
                      <span class="text-sm font-bold text-gray-700">{currency}</span>
                    </div>
                  ))}
                </div>
              </div>
            )}

            <!-- 重要提示 -->
            <div class="mt-8 bg-gradient-to-r from-amber-50 to-orange-50 border-2 border-amber-200 rounded-3xl p-8">
              <div class="flex items-start">
                <div class="w-8 h-8 bg-amber-500 rounded-xl flex items-center justify-center mr-4 mt-1 flex-shrink-0">
                  <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                  </svg>
                </div>
                <div>
                  <h5 class="text-lg font-semibold text-amber-800 mb-3">费用说明</h5>
                  <p class="text-amber-700 leading-relaxed">
                    具体费用可能因地区、充值方式和使用情况而有所不同。建议在申请前详细了解最新的费用标准。汇率转换费用通常按照实时汇率计算。
                  </p>
                </div>
              </div>
            </div>
          </section>

          <!-- Pros and Cons -->
          <section class="bg-white rounded-3xl shadow-xl p-8">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">优缺点分析</h2>
            <div class="grid md:grid-cols-2 gap-8">
              
              <!-- Pros -->
              <div>
                <h3 class="text-xl font-bold text-green-600 mb-4 flex items-center">
                  <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  优点
                </h3>
                <ul class="space-y-3">
                  {pros?.map((pro) => (
                    <li class="flex items-start">
                      <svg class="w-5 h-5 text-green-500 mr-3 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                      </svg>
                      <span class="text-gray-700">{pro}</span>
                    </li>
                  ))}
                </ul>
              </div>

              <!-- Cons -->
              <div>
                <h3 class="text-xl font-bold text-red-600 mb-4 flex items-center">
                  <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  缺点
                </h3>
                <ul class="space-y-3">
                  {cons?.map((con) => (
                    <li class="flex items-start">
                      <svg class="w-5 h-5 text-red-500 mr-3 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                      </svg>
                      <span class="text-gray-700">{con}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </section>

          <!-- Features -->
          {features && features.length > 0 && (
            <section class="bg-white rounded-3xl shadow-xl p-8">
              <h2 class="text-3xl font-bold text-gray-900 mb-8">特色功能</h2>
              <div class="grid md:grid-cols-2 gap-4">
                {features.map((feature) => (
                  <div class="flex items-center p-4 bg-indigo-50 rounded-xl">
                    <svg class="w-6 h-6 text-indigo-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    <span class="text-gray-800 font-medium">{feature}</span>
                  </div>
                ))}
              </div>
            </section>
          )}

          <!-- Giscus Comments Section -->
          <GiscusComments title={`${name} 用户评论`} />
        </div>

        <!-- Sidebar -->
        <div class="space-y-8">
          <!-- Quick Info Card -->
          <div class="bg-white rounded-3xl shadow-xl p-8 sticky top-24">
            <h3 class="text-2xl font-bold text-gray-900 mb-6">基本信息</h3>
            
            <div class="space-y-4">
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">发卡机构</span>
                <span class="font-medium text-gray-900">{issuer}</span>
              </div>
              
              <div class="flex justify-between items-center py-2 border-b border-gray-100">
                <span class="text-gray-600">卡片类型</span>
                <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                  cardType === 'visa' ? 'bg-blue-100 text-blue-800' : 'bg-red-100 text-red-800'
                }`}>
                  {cardType?.toUpperCase()}
                </span>
              </div>
              
              {virtualCardPrice && (
                <div class="flex justify-between items-center py-2 border-b border-gray-100">
                  <span class="text-gray-600">虚拟卡费用</span>
                  <span class="font-medium text-gray-900">{virtualCardPrice}</span>
                </div>
              )}
              
              {physicalCardPrice && (
                <div class="flex justify-between items-center py-2 border-b border-gray-100">
                  <span class="text-gray-600">实体卡费用</span>
                  <span class="font-medium text-gray-900">{physicalCardPrice}</span>
                </div>
              )}
            </div>

            <!-- CTA Button -->
            {affiliateLink && (
              <a 
                href={affiliateLink}
                target="_blank"
                rel="noopener noreferrer"
                class="mt-8 w-full block text-center bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-bold py-4 px-6 rounded-xl hover:from-indigo-700 hover:to-purple-700 transition-all transform hover:scale-105"
              >
                立即申请
              </a>
            )}
          </div>
        </div>
      </div>

      <!-- Content Section -->
      {Content && (
        <section class="mt-12 bg-white rounded-3xl shadow-xl p-8">
          <div class="prose prose-lg max-w-none">
            <Content />
          </div>
        </section>
      )}

      <!-- Related Cards -->
      {relatedCards.length > 0 && (
        <section class="mt-12">
          <div class="bg-white rounded-3xl shadow-xl p-8">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">相关推荐</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
              {relatedCards.map((relatedCard) => (
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow">
                  <div class="relative h-48 bg-gradient-to-br from-indigo-500 to-purple-600">
                    <div class="absolute top-4 left-4">
                      <span class={`px-3 py-1 rounded-full text-xs font-medium ${
                        relatedCard.data.cardType === 'visa' ? 
                        'bg-blue-100 text-blue-800' : 'bg-red-100 text-red-800'
                      }`}>
                        {relatedCard.data.cardType?.toUpperCase()}
                      </span>
                    </div>
                  </div>
                  
                  <div class="p-6">
                    <h3 class="text-xl font-bold mb-2 hover:text-indigo-600 transition-colors">
                      <a href={`/cards/${relatedCard.slug}`}>
                        {relatedCard.data.name}
                      </a>
                    </h3>
                    
                    <p class="text-gray-600 text-sm mb-4">{relatedCard.data.description}</p>
                    
                    <div class="flex items-center justify-between">
                      <div class="flex items-center space-x-2">
                        {relatedCard.data.isVirtual && (
                          <span class="px-2 py-1 bg-purple-100 text-purple-600 text-xs rounded-full">虚拟卡</span>
                        )}
                        {relatedCard.data.isPhysical && (
                          <span class="px-2 py-1 bg-indigo-100 text-indigo-600 text-xs rounded-full">实体卡</span>
                        )}
                      </div>
                      
                      <a 
                        href={`/cards/${relatedCard.slug}`}
                        class="text-indigo-600 hover:text-indigo-800 text-sm font-medium"
                      >
                        查看详情 →
                      </a>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      )}

      <!-- Usage Limits Section -->
      {limits && limits.length > 0 && (
        <section class="mt-12">
          <div class="bg-white rounded-3xl shadow-xl p-8">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">使用限制</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              {limits.map((limit) => (
                <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-6">
                  <div class="flex items-start">
                    <svg class="w-6 h-6 text-yellow-600 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z"/>
                    </svg>
                    <div>
                      <p class="text-yellow-800 font-medium">{limit}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      )}

      <!-- Rewards Section -->
      {rewards && rewards.length > 0 && (
        <section class="mt-12">
          <div class="bg-white rounded-3xl shadow-xl p-8">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">奖励计划</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              {rewards.map((reward) => (
                <div class="bg-gradient-to-br from-green-50 to-emerald-50 border border-green-200 rounded-xl p-6">
                  <div class="flex items-center mb-4">
                    <div class="w-12 h-12 bg-green-500 rounded-xl flex items-center justify-center mr-4">
                      <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
                      </svg>
                    </div>
                    <h3 class="text-lg font-semibold text-green-800">奖励</h3>
                  </div>
                  <p class="text-green-700">{reward}</p>
                </div>
              ))}
            </div>
          </div>
        </section>
      )}

    </div>
  </div>
</Layout>

<style>
  /* 自定义样式 */
  .prose {
    max-width: none;
  }
  
  .prose h1,
  .prose h2,
  .prose h3,
  .prose h4 {
    color: #1f2937;
  }
  
  .prose p {
    color: #4b5563;
    line-height: 1.8;
  }
  
  .prose a {
    color: #4f46e5;
    text-decoration: none;
  }
  
  .prose a:hover {
    text-decoration: underline;
  }
  
  .prose ul {
    list-style-type: disc;
    padding-left: 1.5rem;
  }
  
  .prose ol {
    list-style-type: decimal;
    padding-left: 1.5rem;
  }
  
  .prose li {
    margin: 0.5rem 0;
  }
  
  .prose blockquote {
    border-left: 4px solid #e5e7eb;
    padding-left: 1rem;
    font-style: italic;
    color: #6b7280;
    margin: 1.5rem 0;
  }
  
  .prose code {
    background-color: #f3f4f6;
    padding: 0.25rem 0.5rem;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    color: #1f2937;
  }
  
  .prose pre {
    background-color: #1f2937;
    color: #f9fafb;
    padding: 1.5rem;
    border-radius: 0.75rem;
    overflow-x: auto;
    font-size: 0.875rem;
    line-height: 1.7;
  }
  
  .prose pre code {
    background: none;
    color: inherit;
    padding: 0;
  }
  
  .prose table {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
  }
  
  .prose th,
  .prose td {
    padding: 0.75rem;
    border: 1px solid #e5e7eb;
    text-align: left;
  }
  
  .prose th {
    background-color: #f9fafb;
    font-weight: 600;
  }
  
  .prose img {
    border-radius: 0.75rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }
</style>